name: CI

on: [push]

jobs:
  cfn-lint:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: cfn-lint
      uses: ScottBrenner/cfn-lint-action@1.1.0
      with:
        args: "folder/**/*.yaml"

